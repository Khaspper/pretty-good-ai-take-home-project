[project]
name = "pretty-good-ai"
version = "0.1.0"
description = "Pretty GOod AI caller simulator for a healthcare front office voice agent"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "python-dotenv>=1.0",
    "twilio>=9.3",
    "websockets>=13.0",
]
